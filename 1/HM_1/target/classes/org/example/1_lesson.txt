--Values, Variables, Arrays etc.
1: 25 - 62
    1.Добиться безопасности и переносимости сетевых программ позволяет генерируемый компилятором Java код, не я вляющийся исполняемым.
    Такой код называется байт-кодом. Это оптимизированный н абор команд, предназначенных
    для выполнения в исполняющей среде , называемой виртуальной машиной Java
    (Java Virtual Machine - JVM). В иртуальная машина Java фактически представляет собой интерпретатор байт-кода.
    2.Наследование, полиморфизм, инкапсуляция
    3.С main
    4.блок памяти хранящий в себе значение
    5.67count
    6.// or /*..*/
    7.
        if(условие) { тело } else(другое условие) { тело }
        for(начало итерации, условие, шаг) { тело }
    8.любое тело в {}
    9.GalToLit
    10.GalToLit
    11.Если при консольном вводе то экзепшены о типах данных, если
    12.Да, имеет
2: 63 - 98
    1.Для переносимости программ
    2.Используется кодировка unicode и не является 8-битным. Знак=число char
    3.Может иметь только два значения: true or false
    4.Sound
    5.Переменная объявлена в теле, а не за ним, не будет видно
    6.порядком выполнения, префиксное - до использования, постфиксное - после
    7.Он вычисляет только когда небходимо
    8.byte -> int, short -> double
    9.Когда необходимо привести к другому виду переменной
    10.Sound
    11.Нет, не влияют
    12.Да, определяет
3: 99 - 138
    1.Cycle3
    2.if(условие) { тело } else(другое условие) { тело } else if (условие) { тело }
    3.2
    4.Cycle3
    5.Нет, не будет видеть i
    6.break выходит либо полностью из цикла либо из помеченого цикла one:
    7.После while
    8.Два столбца четных и нечетных чисел включая 0
    9.Cycle3
    10.Cycle3
    11.Непрерывный цикл
    12.Нет, достаточно указать где должно продолжиться выполнение меткой
5: 171 - 218
    1.int array[] = new int[10] or int array[] = new int[] {1, 2, 3}
    2.int array[] = new int[] {1, 2, 3, 4, 5}
    3.Arrays5
    4.Arrays5
    5.IndexOf берет первых входящий, LastIndexOf берет второй входящий
    6.через точку после названия переменной
    7.Arrays5
    8.Не применимо
    9.Arrays5
    10.Логическое, т.к. не происходит операций
    11.При отрицательном индексе будет выход за границы массива, так же как и превышение границы массива
    12.>>>
    13.Arrays5
    14.Думаю нет, т.к. в foreach цикл идет по порядку массива, а в for можно начать с конца
    15.Можно